#include "main.h"

/**
 * print_alphabet - This function is use to prints the alphabet in lowcase
 *
 */

void print_alphabet(void)
{
	int i;

	for (i = 'a'; i <= 'z'; i++)
	{
		_putchar(i);
	}
	_putchar('\n');
}
