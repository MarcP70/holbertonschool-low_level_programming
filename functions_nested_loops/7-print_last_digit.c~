#include "main.h"
/**
 * print_last_digit - function that prints the last digit of a number
 * @n: number input
 *
 * Return: last digit og a number
 */

int print_last_digit(int n)
{
	int var;

	if (n < 0)
	{
		n = n * -1;
	}
	var = n % 10;
	_putchar('0' + var);
	return (var);
}
