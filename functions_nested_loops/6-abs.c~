#include "main.h"
/**
 * _abs - function that computes the absolute value of an integer
 * @int: number input
 *
 * Return: absolute number on output
 */

int _abs(int)
{
	int var;

	if (int >= 0)
	{
		var = int;
	} else
	{
		var = int * -1;
	}
	return (var);
}
