#include "main.h"

/**
 * rev_string - This function reverses a string
 *
 * @s: pointer of string
 *
 */

void rev_string(char *s)
{
	int len = 0;
	int index = 0;

	while (s[index++])
	{
		len++;
	}

	for (index = len - 1; index >= 0; index--)
	{
		_putchar(s[index]);
	}
	_putchar('\n');
}
